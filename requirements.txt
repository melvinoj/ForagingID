# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
jinja2==3.1.4

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0
alembic==1.14.0

# Image processing
Pillow==11.0.0
pillow-heif==0.21.0
imagehash==4.3.1

# EXIF parsing
exifread==3.0.0
piexif==1.1.3

# HTTP client (async, used for iNaturalist/GBIF future integrations)
httpx==0.27.2
# Async HTTP client (used by DeepSeek + Ollama draft backends)
aiohttp>=3.9.0
# Sync HTTP (used for PlantNet multipart upload — more reliable for files+data)
requests==2.32.5

# CLI
click==8.1.7

# Config
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# Geo utilities
geopy==2.4.1

# Utilities
tqdm==4.67.1
aiofiles==24.1.0
greenlet==3.1.1

# HTML parsing (PFAF scraping for culinary enrichment)
beautifulsoup4==4.12.3

# Anthropic Claude API — for AI-drafted species fields (taste notes, medicinal notes, recipe)
anthropic>=0.40.0

# QR code generation (Connections page — server-side, no CDN dependency)
qrcode[pil]==8.0

# PDF generation (Phase 12 — participant copy download)
weasyprint==66.0
